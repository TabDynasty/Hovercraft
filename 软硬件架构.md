# 数学推导
我认为这篇文章在 Section II.F 部分需要更加完整的数学推导。
$$
\begin{aligned}
    L & = \int_{t_s}^{t_g} \vert\vert W^Ts'\vert\vert_2 dt - \lambda_t\int_{t_s}^{t_g} \vert\vert W^Ts - f\vert\vert_2^2 dt \\ 
    &= \int_{t_s}^{t_g} \sqrt{s'^TWW^Ts'} dt - \int_{t_s}^{t_g} \lambda_t(s^TWW^Ts -2W^Tsf +f^2) dt \\ 
    \frac{\partial L}{\partial W} &= \int_{t_s}^{t_g} \frac{\partial\sqrt {s'^TWW^Ts'}}{\partial W} dt - \int_{t_s}^{t_g} \lambda_t\frac{\partial(s^TWW^Ts -2W^Tsf +f^2)}{\partial W} dt  \\ 
    &= \int_{t_s}^{t_g} \frac{W^Ts'}{\sqrt {s'^TWW^Ts'}}\frac{\partial (W^T s')}{\partial W} dt - \int_{t_s}^{t_g} \lambda_t\frac{\partial(sW^TWs -2W^Tsf +f^2)}{\partial W} dt \\ 
    &= \int_{t_s}^{t_g} \frac{s'^TsW}{\sqrt {s'^TWW^Ts'}} dt + \int_{t_s}^{t_g} \lambda_t(-ss^TW +2sf^T)dt 
\end{aligned}
$$

# 软硬件架构
### 硬件平台
可以选择ROS作为实验的开发环境，因为ROS集成了大部分的传感器的相关接口，代码部分使用C++，提高运行效率。

### 软件架构
***
- ### 初始化：
1. 手眼标定以确定$H^c_g$, CAD模型确定$H_a^b$, 同时根据初始机械臂姿态以及AMR的初始位置确定$H^g_b,H_a^w$
2. 人为确定重构过程路径点 $f$, 用于后续的路径规划
3. 确定划分工作区域的距离阈值$H,L$
***
- ### 程序运行( 循环 )： 

> 1. 更新4个坐标变换矩阵   
> 2. 使用**梯度下降法**更新$$W$$矩阵，更新机器人路径. **条件：测试件的距离与当前位置的距离平方 > AMR的实际长度$\epsilon$，待测件在相机坐标系中距离平方 > $d_v$，待测件在世界坐标系中坐标与$P_{gs}$之间的距离平方 > AMR的长的一半(碰撞检测)**
``` 
D > H
1. 将相机拍摄到2D图像应用深度信息转换为物体在相机坐标系中的位置坐标, 将坐标变换到世界坐标系, 同时点云估计待测物体的长宽高。
2. 计算机器人扫描多视图，进行运动规划

L < D < H
1. 利用相机不同角度拍摄的深度图片，进行伪标签的生成，映射为3D，再透视投影到2D
2.  根据轮廓完成伪标签的扩展和选择
3.  根据最近边界点寻找最小凸包，重复得到完整3D模型。

D < L
1. 通过3D模型进行主成分分析，确定热像仪的位姿 
2. 拍摄红外图像
```
> 1. <!--根据更新后的路径以及热像仪位姿-->，输入进控制器，对移动机器人底盘以及机械臂进行控制。
> 2. 红外图像与3D模型结合得到3D红外模型，进行故障分析

***